1、txt数据不适合直接读取使用，我们获取的txt数据是从数据库中导出的，所以最好的办法就是再倒入数据库，然后从数据库获取数据，所以采取txt数据--》mysql--》php读取数据，然后将读取到的数据写入到js文件中去，直接在html中用js调用即可，见businfo.js和stationinfo.js两个文件，
如果对php和mysql不感兴趣，可以忽略php结尾的文件，直接使用这两个js文件即可，文件中的汉字是编码过的，直接调用就行。
ps：数据的sql文件已经导出，在databaseSQL文件夹中。
2、显示效果的话，一个是所有公交站点的信息，我通过折线连接了起来，很不友好，这个可能和数据本身有关系。第二是某个公交车经过一系列站点的时间和坐标信息，可以以移动的方式刻画出来，但没有公交线路的地图，也不太友好。
3、根据分析的数据未曾找到像你所的那种结果，进行结果的分析和对比，可能是我没看出来，你再给指导指导。

4、大部分时间在处理数据了，你妹的。（原来的坐标和百度本身的坐标有偏移，其实就是原来的坐标不精确，不得已把百度的公交站点的坐标获取过来，然后把数据库中不精确的坐标给更新一遍，再加载infowindow就准确多了）。